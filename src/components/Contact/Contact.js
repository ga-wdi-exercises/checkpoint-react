constructor(props);
{
  super(props);

  this.state = {
    contacts: props.contacts
  };
}

render();
{
  <div className="Contact" />;
  this.state.profile_picture = state.profile_picture;
  this.state.name = this.state.name;
  this.state.email = this.state.email;

  return (
    <div>
      <h2>Profile Picture</h2>
      <h3>Name</h3>
      <h4>email</h4>
    </div>
  );
}

export default Results;
